import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './LoginPage.css' // ou crie um global.css

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
